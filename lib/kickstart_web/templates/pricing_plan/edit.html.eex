<h1>Edit Pricing plan</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.pricing_plan_path(@conn, :update, @pricing_plan)) %>

<span><%= link "Back", to: Routes.pricing_plan_path(@conn, :index) %></span>
